#include<stdio.h>
int main()
{
    int arr[]={8,7,6,5,4};
    int key,n,i;
    for(i=0;i<n;i++)
    key=7;
    if(arr[i]==key)
        printf("elements not found");
    else
       printf("element found in %d");
    return 0;
}